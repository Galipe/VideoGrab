/* ──────────────────────────────────────────────
   VideoGrab — config.js
   ÚNICO lugar para definir a URL do servidor na nuvem.

   👉 Depois de fazer o deploy (Render/Railway), troque a URL abaixo
      pela URL pública do seu servidor (sem barra no final).
      Ex.: "https://videograb-abc123.onrender.com"

   ⚠️  Se mudar o domínio, atualize também "host_permissions"
       no manifest.json para o mesmo endereço.
   ────────────────────────────────────────────── */

const VIDEOGRAB_SERVER = "https://videograb-kbqc.onrender.com";
